import { colors } from "@/shared/config/tokens";

export const tooltipStyle = {
  background: colors.surface,
  border: `1px solid ${colors.border}`,
  borderRadius: 12,
  fontSize: 12,
  color: colors.text,
};

export const tooltipTextStyle = { color: colors.text };
